

Update lesson completion progress settings:
    Save lesson completion progress across devices (if cloud sync is enabled)
    
    When a section or module is marked as finished, update screen, now its like screen is not updating, the cause maybe because the widget mightbe stateful and state variable isn't updated.


In error alerts, show only fatel errors( like the ones that can get app killed) the others are just annoying


Implement Spaced Practice:
    Stage1:
        Bookmark Lessons:
            Window for Bookmarks:
                Sort bookmarks by date and show date of last open on the window
            Bookmark button on lesson screen

