gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Exception: the project-wide debug keystore is intentionally committed so
# every machine and CI runner signs builds with the same key (no per-dev
# setup, installs upgrade cleanly). Do NOT commit real release keystores.
!app/debug.keystore
